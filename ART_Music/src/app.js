App({
    page: "pages/music_page/music_page",

    /* app 加载完成触发该函数 */
    onLaunch: function(event) {},

    /* app 可见时触发该函数 */
    onShow: function(event) {},

    /* app 不可见时触发该函数 */
    onHide: function(event) {},

    /* app 退出触发该函数 */
    onExit: function(event) {},
});